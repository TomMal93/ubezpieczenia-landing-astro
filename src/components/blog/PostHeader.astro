---
interface Props {
  title: string;
  date?: string;
  tags?: string[];
}
const { title, date, tags = [] } = Astro.props as Props;
---

<header class="mb-8">
  <h1 class="text-4xl sm:text-5xl font-bold">{title}</h1>
  {date && <p class="mt-2 muted">{date}</p>}
</header>
