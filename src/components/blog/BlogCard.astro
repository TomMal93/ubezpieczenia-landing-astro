---
interface Props {
  title: string;
  href: string;
  excerpt?: string;
  cover?: { src: string; alt?: string; width?: number; height?: number };
  date?: string;
  readingTime?: number;
}
const { title, href, excerpt, cover, date, readingTime } = Astro.props as Props;
---

<a href={href} class="block group card overflow-hidden">
  {
    cover && (
      <img
        src={`${cover.src}?auto=format&fit=max&w=800`}
        alt={cover.alt ?? ''}
        width="800"
        height="450"
        loading="lazy"
        style="aspect-ratio:16/9;object-fit:cover;width:100%;display:block"
      />
    )
  }
  <div class="p-5 sm:p-6">
    <h3 class="text-xl font-semibold group-hover:underline">{title}</h3>
    {excerpt && <p class="mt-2 muted">{excerpt}</p>}
    <p class="mt-3 text-sm muted">
      {date && <span>{date}</span>}
      {readingTime && <span> · {readingTime} min czytania</span>}
    </p>
  </div>
</a>
